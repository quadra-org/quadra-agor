/**
 * Copilot Model Constants
 *
 * Re-exports from @agor/core — single source of truth for model definitions.
 */
export {
  COPILOT_CONTEXT_LIMITS,
  COPILOT_MODEL_METADATA,
  COPILOT_MODELS,
  type CopilotModel,
  DEFAULT_COPILOT_MODEL,
  getCopilotContextWindowLimit,
} from '@agor/core/models';
